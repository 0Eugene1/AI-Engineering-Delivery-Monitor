/**
 * Jira integration (polling, later optional webhook).
 *
 * <p>Not implemented yet — this is a placeholder package prepared during the
 * Skeleton phase so the next task can start directly with Jira integration.
 *
 * <p>See {@code docs/architecture.md} (Backend packages table),
 * {@code docs/integrations.md} and {@code docs/discovery.md} (§9.1, confirmed
 * project key {@code MPTPSUPP}, board 718, filter 30532) for scope and config.
 */
package ru.eltc.deliverymonitor.integration.jira;
